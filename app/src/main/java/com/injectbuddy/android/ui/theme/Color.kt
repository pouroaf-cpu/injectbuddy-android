package com.injectbuddy.android.ui.theme

import androidx.compose.ui.graphics.Color

// Brand teal — matches the web app accent (#0fbcad).
val Teal = Color(0xFF0FBCAD)
val TealDark = Color(0xFF0A8C81)
val TealContainerLight = Color(0xFFB8F2EC)
val TealContainerDark = Color(0xFF00504A)

// Neutral surfaces tuned to the web app's near-black dark mode + clean light mode.
val LightBackground = Color(0xFFF7F8F8)
val LightSurface = Color(0xFFFFFFFF)
val LightOnSurface = Color(0xFF14141C)

val DarkBackground = Color(0xFF0E0F11)
val DarkSurface = Color(0xFF17191C)
val DarkOnSurface = Color(0xFFF0F0F2)

val ErrorRed = Color(0xFFD3473F)
